// Bán kính mặc định cho tab "Quanh đây" (GPS sống) — dùng chung giữa feed.tsx (feed thật) và
// done.tsx (màn tổng kết onboarding, hiện trước khi vào feed) để 2 nơi không lệch số với nhau.
export const NEARBY_RADIUS_KM = 1.5;
